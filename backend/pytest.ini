[pytest]
asyncio_mode = auto
testpaths = tests
pythonpath = app
addopts = 
    -v
    --strict-markers
    --cov=app
    --cov-report=term-missing
    --cov-report=html

markers =
    integration: requires PostgreSQL/Redis
    unit: pure unit tests, no database
